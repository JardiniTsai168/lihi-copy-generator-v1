## 2026-08-01 Copy / Creative Split

- 使用者明確要求：`copy.bktsai.link` 之後不要再動；素材功能與後續開發只在 `creative.bktsai.link` 處理。
- `creative` 已獨立成新 repo：`JardiniTsai168/lihi-copy-generator-creative-v1`
- 本機 creative repo 路徑：`/Users/tonytsai/.openclaw/workspace-lihi-copy-generator-creative-v1`
- 已將 creative 相關程式同步進獨立 repo。
- creative repo 的 `scripts/deploy-live.sh` 已封鎖，避免誤 deploy 到 `copy`；之後 creative 只能走 `scripts/deploy-creative-live.sh`。
- 當時已驗證 creative repo：`node --check` 通過，`npm test` 30/30 pass。

## 2026-08-01 Creative Material Feature Audit

- 使用者要求以現有 `creative.bktsai.link` 頁面為基礎，把右側改成主 tab：`產出文案` / `產出素材`。
- 素材流程目標：主文案自動帶入素材 tab，讓使用者選 `風格`、`模特兒`，先產 `Facebook 1200x600` 主圖，再延伸 `IG / Threads / Google Ads`。
- 曾經部署過一版素材 UI 到 `creative.bktsai.link`，但後續 audit 發現 live bug：前端打 `POST /generate-creative`，實際 live bridge 沒有這條路由，production 會回 `404 Cannot POST /generate-creative`。
- audit 結論：素材功能當時只接在 `src/worker.js` / `server.js` 路徑，沒接進 live 使用的 `bridge-server.js`；而且當時生成的是 deterministic SVG placeholder，不是真正 AI 圖像生成。
- 後續若修素材功能，應只在獨立的 creative repo 上處理，不能影響 `copy.bktsai.link`。

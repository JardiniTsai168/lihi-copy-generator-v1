# 2026-07-18

## 決策

- 準備將 `beck-v1` 與 `bridge-server.js` 一起部署到 DigitalOcean Droplet，正式對外網域改用 `copy.bktsai.link`。
- 部署版本以「不依賴 OpenClaw」為準，`bridge-server.js` 改走 `OPENAI_API_KEY + OPENAI_MODEL + OPENAI_BASE_URL` 直接模型呼叫。
- `Meta 廣告` 渠道輸出改為固定包含 `Headline / Primary text / Description / CTA / URL`，不再只產 `Headline / Primary text / CTA / URL`。
- `SMS` 渠道輸出新增硬限制：`title + body + cta` 三欄位總字元數不得超過 `70`，`url` 不列入字數。

## 驗證

- DigitalOcean Ubuntu 主機已完成 `node v22.23.1`、`pm2 7.0.3`、`tesseract 5.3.4` 安裝。
- 已確認目前部署目標不再依賴 OpenClaw gateway module，VPS 無需安裝 `openclaw` 或檢查 gateway module 路徑。
- `node --check bridge-server.js` 已通過。
- 已從此 workspace 以 `~/.ssh/id_ed25519` 成功連入 `root@157.245.151.126`，確認先前阻塞點是本地私鑰 passphrase 尚未載入 `ssh-agent`。
- 專案已同步到 droplet 的 `/var/www/beck-v1`，`npm install` 已完成。
- 已建立 bridge `.env` 預設值、用 `pm2` 啟動 `beck-v1`，並完成 `pm2-root` systemd 開機自啟設定。
- `nginx` 已反向代理 `copy.bktsai.link -> 127.0.0.1:3456`。
- 已成功申請並部署 Let's Encrypt 憑證，`https://copy.bktsai.link/health` 可回 `200 OK`。
- 目前 bridge 因未配置 `OPENAI_API_KEY`，health 顯示 `mode: mock`。
- 已補上圖片型銷售頁解析增強：除了本機 `tesseract` OCR，bridge 也會用 OpenAI vision 直接分析前幾張商品圖，抽取可驗證的圖片文案、賣點與包裝線索。
- 已將頁面摘要改為優先採用圖片視覺賣點，不再直接把原始 OCR 雜訊塞進前端主要摘要區。
- `node --check bridge-server.js`、`node --check public/app.js` 已通過，確認本次欄位與字數限制調整沒有語法錯誤。

## 待辦

- 補上正式 `OPENAI_API_KEY`，並視需要設定 `OPENAI_MODEL`、`BRIDGE_API_KEY`。
- 更新 `pm2` 服務環境後重啟 `beck-v1`，確認 `/health` 由 `mock` 轉為 `live`。
- 持續調整 OCR 雜訊過濾與圖片賣點去重，避免 `BELFORT / GOURMET / 南瓜濃湯` 這類重複或低價值訊號佔掉摘要配額。
- 補一次實際 `Meta 廣告` 與 `SMS` 產出驗收，確認 live model 也會穩定填滿 `Description`，且 SMS 三欄合計持續落在 `70` 字內。

# 2026-07-16

## 決策

- 建立「lihi 實驗室文案產生器 V1」獨立頁面原型。
- 第一版輸入欄位固定為產品名稱、3 到 5 個優點、產品頁連結、文案風格。
- 第一版輸出格式固定為標題、主文、CTA、連結。
- 先不做登入、歷史紀錄、儲存。
- agent 串接層先採 mock / live 雙模式，方便先驗證前端體驗，再接真實貝克 v1。
- 補上 static demo 降級策略，讓 GitHub Pages 在沒有後端時也能正常展示流程。

## 待辦

- 確認貝克 v1 真實 endpoint 規格與認證方式。
- 若要公開測試 live 版，需要一個可安全保存祕鑰的後端主機，不能只靠 GitHub Pages。
- 補上更明確的輸出長度限制與品牌語氣規則。
- 決定是否需要從產品頁抓取更多上下文再交給 agent。

## 驗證

- 已完成本地啟動驗證，`npm start` 可正常提供頁面與 API。
- `GET /api/health` 回傳 mock 模式狀態正常。
- `POST /api/generate-copy` 可正常回傳 mock 文案結果。
- 無效輸入時，API 會正確回傳欄位驗證錯誤。
- `BECK_V1_MODE=openclaw npm start` 已驗證可透過本機 OpenClaw 真實呼叫 `beck-v1`。
- GitHub repo 已建立：`JardiniTsai168/lihi-copy-generator-v1`
- GitHub Pages 已成功部署：<https://jardinitsai168.github.io/lihi-copy-generator-v1/>
- live 對外測試版已透過 Cloudflare named tunnel 驗證可從公開網址直連本機 `beck-v1` 後端。

## 備註

- GitHub Pages 版因為是純靜態站，目前會自動降級為 static demo mock 模式。
- 若要公開可用的 live 版，需要一個可安全保存祕鑰、並能在 server-side 呼叫 `beck-v1` 的後端主機。
- 目前測試 live 網址仰賴本機常駐的 node server 與 cloudflared tunnel session，屬於測試用途，不是正式 production 佈署。

# 2026-07-20

## 驗證

- 已透過 SSH 連到正式站 `root@157.245.151.126`，在 `/var/www/beck-v1/.env` 補上 `SCREENSHOTONE_ACCESS_KEY` 與 `SCREENSHOTONE_ENABLED=true`。
- 已執行 `pm2 restart beck-v1 --update-env` 重啟正式站服務。
- 已驗證 `<https://copy.bktsai.link/health>` 回 `{\"ok\":true,\"service\":\"beck-copy-engine\",\"mode\":\"live\",\"provider\":\"openai\",\"model\":\"gpt-5.6-terra\",\"framework\":\"beck-copy-framework-v1\"}`，代表正式站已正常在線。
- 追查 `detailed` 仍顯示 `頁面抓取失敗 (403)` 時，確認先前其實有兩層問題：一是正式站 `.env` 當下未真正保存 `SCREENSHOTONE_ACCESS_KEY`，已補回；二是補回後實測該葉黃素頁仍會讓 ScreenshotOne 回 `host_returned_error`，訊息指出目標 host 未回 `200-299`，也就是 ScreenshotOne 自己同樣被站方擋下。
- 已修正 `bridge-server.js` 的 ScreenshotOne 請求，改為在 JSON body 一併帶入 `access_key`，並擴充錯誤訊息讀取 `error_message`，避免只看到不完整的 fallback 失敗原因。
- `generate-copy` 已做瘦身版並部署到 production：`MAX_PRODUCT_IMAGES=10`、`OCR_ALL_IMAGES=false`、`OCR_IMAGE_LIMIT=3`、`VISION_IMAGE_LIMIT=2`、`SCREENSHOT_VISION_LIMIT=1`、`SCREENSHOT_OCR_LIMIT=1`、`SCREENSHOTONE_USE_SLICES=false`；HTML 文字訊號足夠時會直接跳過 OCR，vision 也只留 1 張主圖。
- 已將新版 `bridge-server.js` 與上述瘦身參數部署到 `root@157.245.151.126:/var/www/beck-v1`，並完成 `pm2 restart beck-v1 --update-env`。
- 實測 `<https://copy.bktsai.link/generate-copy>` 針對 `https://belfortsky.com/product/pumpkin` 已從先前約 `60s` 的 `504 Gateway Time-out` 降到 `HTTP 200 total=19.60s`，確認前端不該再因 upstream timeout 回落到 mock。
- 針對 `LINE` tab 缺失追查後，確認不是瀏覽器快取：正式站 `/var/www/beck-v1/public/index.html` 實際仍是舊版 `20260719j`，HTML 少了 `tab-line`。
- 已將本地 `public/index.html`、`public/app.js`、`public/styles.css`、`public/config.js` 與 `public/detailed.html` 重新同步到 `root@157.245.151.126:/var/www/beck-v1/public/`。
- 重新驗證 `<https://copy.bktsai.link/>` 已直接回新版 `20260719k` HTML，且 `LINE` tab 已出現在 `SMS` 與 `Email` 之間。
- 已新增 `Google Ads` 渠道 tab，前後端 channel key 為 `google_ads`；目前欄位映射為 `Headline / Description / Display path 1 / Display path 2 / Final URL`。
- `Google Ads` formatter 已加上字數限制：`Headline <= 30`、`Description <= 90`、`Display path 1 <= 15`、`Display path 2 <= 15`，並沿用價格詞封鎖規則。
- 已同步部署新版 `public/index.html`、`public/app.js`、`bridge-server.js` 到 `root@157.245.151.126:/var/www/beck-v1`，並完成 `pm2 restart beck-v1 --update-env`。
- 已驗證正式站首頁直接回 `tab-google-ads` 與新版資產版本 `20260720a`，且遠端 `app.js` 已包含 `google_ads` channel 邏輯。
- 已修正 `Google Ads` 渠道長度限制未被穩定遵守的問題：新增 `Google Ads formatter` 專用 prompt，並將 `Headline / Description / Display path 1 / Display path 2` 全部改為硬裁切，不再沿用會補 `…` 的通用 `truncateText()`。
- `Display path` 正規化後改走 `truncateTextHard(..., 15)`，`Description` 改走 `truncateTextHard(..., 90)`；目標是避免 live formatter 或 fallback 在 UI 上產出超出 `90 / 15 / 15` 的欄位值。
- 已更新正式站前端資產版本到 `20260720b`，避免瀏覽器繼續命中舊的 `app.js?v=20260720a`。
- 已驗證正式站首頁直接回 `config.js?v=20260720b` 與 `app.js?v=20260720b`，且遠端 `app.js` 已包含 `truncateTextHard` 的 `google_ads` 邏輯。
- 產品頁網址輸入已改成接受裸網域或裸路徑，例如 `example.com/product`；前端 submit 前會自動補成 `https://example.com/product`，後端 `normalizeInput/normalizeFormatInput` 也會再兜底補協定。
- `public/index.html` 的產品頁欄位已由 `type="url"` 改為 `type="text"`，placeholder 改成 `example.com/product`，避免瀏覽器原生驗證要求使用者手動輸入 `https://`。
- 已更新正式站前端資產版本到 `20260720c`，並驗證首頁直接回 `config.js?v=20260720c` / `app.js?v=20260720c` 與新網址 placeholder。
- 已修正 `Meta 廣告` live formatter 解析後的 `Primary Text` 排序，原本仍走舊的 `內文 -> 連結`，現在統一改為 `內文 -> CTA -> 連結`，與前端 fallback 顯示一致。
- 已查到 `Meta 廣告` 卡在產出中的直接原因是正式站 `formatter_failed: appendCtaAndUrlToMetaBody is not defined`；已在 `bridge-server.js` 補回 helper。
- 已統一把 `CTA` 和連結之間的拼接改成緊接下一行，不再插入空白段落；目前已套用到 `Meta`、`Email`，以及前端 `SMS/LINE` 的整段複製輸出。
- 已修正 `Email` 內文殘留 `http…` 的問題：`normalizeEmailBodyLength` 與 `appendCtaAndUrlToEmailBody` 會先移除模型殘留的 URL/截斷 URL 片段，再補正式 CTA 與連結。
- `Email` tab 的主旨樣式已降回一般文案大小，不再特別放大或粗體。
- `SMS/LINE` 的 compact 卡片已把 `CTA + 連結` 併成同一段顯示，避免畫面上出現中間空行。

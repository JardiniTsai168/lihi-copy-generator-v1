# 2026-07-25

- 已將 `copy.bktsai.link` 表單的可見「選擇網域」區塊改為隱藏式設定：移除「實驗證明，用自訂網域可增加 30% 以上點擊率！」文案與外露 dropdown，改成在「產品頁連結」輸入框右側新增 `設定網址` 按鈕，點擊後才展開 `網址設定` select。
- 前端修改已落在本機 `public/index.html`、`public/styles.css`、`public/app.js`，並通過 `node --check public/app.js`。
- 嘗試於 2026-07-25 部署到 DigitalOcean `root@157.245.151.126:/var/www/beck-v1/public/` 時失敗；SSH verbose log 顯示 server 接受 `~/.ssh/id_ed25519` 公鑰，但此非互動環境無法從 macOS Keychain 取出私鑰 passphrase，因此無法完成簽名登入。
- 2026-07-25 22:51 CST 重新確認 `ssh-add -l` 已載入 `~/.ssh/id_ed25519` 後，成功將 `public/index.html`、`public/styles.css`、`public/app.js` 重新同步到 `root@157.245.151.126:/var/www/beck-v1/public/`。
- 2026-07-25 22:52 CST 驗證 `<https://copy.bktsai.link/>` 已回新版 HTML，包含 `設定網址` 按鈕與 `網址設定` 區塊；response header `Last-Modified: Sat, 25 Jul 2026 14:51:56 GMT`。
- 2026-07-25 22:59 CST 再微調 `copy.bktsai.link` 桌機版雙欄比例：左側輸入表單放寬、右側輸出卡片縮窄，讓 output panel 更接近 `Email` tab 的實際內容寬度。
- 2026-07-25 22:59 CST 補上複製 icon 的可見成功回饋：點擊後按鈕會短暫變成綠色並顯示 `✓`，避免「其實有複製成功但看起來沒反應」。
- 2026-07-25 22:59 CST 已重新同步 `public/styles.css`、`public/app.js` 到 `root@157.245.151.126:/var/www/beck-v1/public/`，正式站 response header 更新為 `Last-Modified: Sat, 25 Jul 2026 14:59:04 GMT`。
- 2026-07-25 23:07 CST 再修正 `copy.bktsai.link` 右側結果 tabs：將桌機版左右欄比例調整為左 `1.08fr` / 右 `0.92fr`，讓右欄略放寬，並把 `result-tabs` 改為不換行、可橫向捲動備援，同時將 tab 的字級與 padding 微縮，避免 `LINE / Email` 斷到下一行。
- 2026-07-25 23:07 CST 已重新同步 `public/styles.css` 到 `root@157.245.151.126:/var/www/beck-v1/public/`，正式站 `styles.css` response header 更新為 `Last-Modified: Sat, 25 Jul 2026 15:07:06 GMT`。
- 2026-07-25 23:36 CST 依需求再把 `copy.bktsai.link` 桌機版主欄改成近乎 `50 / 50`：`public/styles.css` 的 `.grid` 由左 `1.08fr` / 右 `0.92fr` 改為左右皆 `1fr`，並保留兩側最小寬 `400px`。
- 2026-07-25 23:36 CST 已重新同步 `public/styles.css` 到 `root@157.245.151.126:/var/www/beck-v1/public/`，正式站 `styles.css` response header 更新為 `Last-Modified: Sat, 25 Jul 2026 15:36:16 GMT`。
- 2026-07-25 23:39 CST 將可展開區塊標題 `補充第 4、5 個優點` 改為 `補充第 4、5 個優點 (越多文案越準)`，更新檔案為 `public/index.html`。
- 2026-07-25 23:39 CST 已重新同步 `public/index.html` 到 `root@157.245.151.126:/var/www/beck-v1/public/`，正式站首頁 response header 更新為 `Last-Modified: Sat, 25 Jul 2026 15:38:54 GMT`。
- 2026-07-25 深夜對 `copy.bktsai.link` / `bridge-server.js` 做安全與防呆 review：確認目前沒有傳統 SQL injection 路徑，但有數個需要優先補的風險，包括 `productUrl` 任意外連導致 SSRF、公開 API 可能因未設 `BRIDGE_API_KEY` 且 `cors()` 全開而被他站濫用、server 端缺少長度限制導致 token 成本放大、以及 prompt injection / 商品資訊不一致時缺少明確防呆。
- 待辦：若要進入修補，優先順序建議為 1. URL allow/deny 規則與私網封鎖 2. 後端 API 認證與 origin 控制 3. server-side length validation 4. mismatch detection 與 prompt-injection guardrail 5. URL scheme 僅允許 `http/https`。
- 2026-07-25 深夜已完成 `copy.bktsai.link` hardening 第一輪並部署到正式站：`bridge-server.js` 新增同站 `Origin/Referer` 保護，`/generate-copy` 與 `/format-copy` 現在無同站來源會回 `403 forbidden_origin`；另外關閉全開的 `cors()`，live header 已不再回 `Access-Control-Allow-Origin: *`。
- 2026-07-25 深夜已收緊網址規則：產品頁連結與模型輸出連結一律正規化成 `https://`、去除 hash/credential、拒絕自訂 port、並封鎖 `localhost`、`.local`、`.internal`、私網 IP、保留網段與解析到私網的 hostname；`fetchWithTimeout()` 也改成手動追 redirect 並在每次 redirect 前重新驗證網址。
- 2026-07-25 深夜已補 server-side 長度限制：`productName <= 80`、每個 `benefit <= 60`、`productUrl <= 300`，`format-copy` 也會驗證 `masterDraft` 欄位長度，避免繞過前端直接灌大 payload 放大 token 成本。
- 2026-07-25 深夜已補 prompt injection guardrail 與 mismatch detection：頁面/OCR/截圖/圖片內容在 prompt 中都以 untrusted data 標記，並新增產品名稱與頁面標題/圖片關鍵字的重疊檢查；若差異過大，前端 status 會提示使用者先確認網址與商品是否一致。
- 2026-07-25 16:55 GMT 實測正式站：
  1. `POST /generate-copy` 無 `Origin` 回 `403`
  2. `Origin: https://copy.bktsai.link` + `productUrl=localhost:8080/test` 回 `400`，訊息為 `網址不可自帶自訂 port`
  3. `Origin: https://copy.bktsai.link` + `productUrl=example.com` 回 `200`
  4. 首頁 `Last-Modified: Sat, 25 Jul 2026 16:53:36 GMT`
  5. `app.js?v=20260725a` `Last-Modified: Sat, 25 Jul 2026 16:53:37 GMT`
- 2026-07-25 深夜已整理 `bridge-server.js` 的 Beck Copy Engine prompt 架構：把感性 / 理性 1 到 5 的詳細規則集中到 `buildMasterDraftSystemPrompt()` 內的 `buildVoiceBalanceFramework()`，改成母稿層為單一來源；`buildSystemPrompt()` 只保留全域規則，`Primary Copy` 改為嚴格遵守母稿，不再重複吃同一份強度設定。
- 2026-07-25 深夜同步調整輸入訊號：`buildMasterDraftUserInput()` 與 `buildChannelFormatterUserInput()` 的感性 / 理性強度改為短格式 `Level N（中文標籤）`；`buildPrimaryCopyUserInput()` 拿掉重複的強度欄位，避免在主文案層二次定義風格。
- 2026-07-25 深夜已部署新版 `bridge-server.js` 到 `root@157.245.151.126:/var/www/beck-v1/bridge-server.js`，並在遠端通過 `node --check bridge-server.js` 後執行 `pm2 restart beck-v1`。
- 2026-07-25 深夜部署驗證：
  1. `curl https://copy.bktsai.link/health` 回 `{"ok":true,"mode":"live","provider":"openai","model":"gpt-5.6-terra"}`
  2. 遠端 `bridge-server.js` 已可 grep 到 `buildVoiceBalanceFramework`、`getVoiceBalanceInputLabel` 與多處 `tone_note` 遵循規則，確認正式站已跑新 prompt 結構。
- 2026-07-25 深夜建立正式站備份點：`root@157.245.151.126:/var/www/beck-v1/backups/2026-07-25-193748-pre-review-backup/`，並另存 tarball `root@157.245.151.126:/var/www/beck-v1/backups/2026-07-25-193748-pre-review-backup.tar.gz`，內容含 `bridge-server.js`、`public/`、`src/`、`package*.json`、`README.md`、`wrangler.jsonc`。
- 2026-07-25 深夜 live review 發現正式站目前與先前 hardening 記錄不一致：`curl -X OPTIONS https://copy.bktsai.link/generate-copy -H 'Origin: https://evil.example'` 仍回 `Access-Control-Allow-Origin: *`，且未帶 auth 直接 `POST /generate-copy`、`POST /format-copy` 會進入 validation 而非 `401`，代表 bridge 仍可被跨站直接調用。
- 2026-07-25 深夜另確認目前 repo / live 存在流程漂移：`bridge-server.js` 的 `/detailed` route 仍存在，但 `public/detailed.html` 已不存在，live `GET /detailed` 回 `404 ENOENT`；另外 `src/worker.js` 只有 `/api/health` 與 `/api/generate-copy`，尚未支援前端實際會打的 `/format-copy`。
- 2026-07-25 深夜已完成第二輪修補並部署到正式站：
  1. `bridge-server.js` 移除全開 `cors()`，改成只保護 `/generate-copy`、`/format-copy`，未帶同站 `Origin/Referer` 且無有效 `BRIDGE_API_KEY` 時一律回 `403 forbidden_origin`
  2. 移除早已失效的 `/detailed` route，避免繼續觸發 `public/detailed.html` 的 `ENOENT`
  3. server-side 補回 URL 正規化與公開網址限制：只允許公開 `https`、拒絕自訂 port、封鎖 `localhost` / `.local` / `.internal` / 私網 IPv4 / 常見私網 IPv6
  4. `fetchWithTimeout()` 改成手動追 redirect，且每次 redirect 前重新驗證目標網址
  5. server-side 補回長度限制：`productName <= 80`、每個 `benefit <= 60`、`productUrl <= 300`，`masterDraft` 各欄位與陣列也一併驗證
- 2026-07-25 深夜正式站修補後驗證：
  1. `OPTIONS https://copy.bktsai.link/generate-copy` 搭配 `Origin: https://evil.example` 現在回 `403 forbidden_origin`
  2. 無 `Origin` 直接 `POST https://copy.bktsai.link/generate-copy` 現在回 `403 forbidden_origin`
  3. `Origin: https://copy.bktsai.link` + `productUrl=http://localhost:8080/test` 現在回 `400 validation_failed`
  4. `Origin: https://copy.bktsai.link` + `masterDraft.url=http://localhost:8080/test` 的 `/format-copy` 現在回 `400 validation_failed`
  5. `GET https://copy.bktsai.link/detailed` 現在是正常的 `Cannot GET /detailed`，不再是 `ENOENT`
  6. `GET https://copy.bktsai.link/health` 仍正常回 `mode: live`、`model: gpt-5.6-terra`
- 2026-07-25 深夜已同步修正 Worker 備援路徑：`src/worker.js` 補上 `/api/format-copy` proxy，並把 mock tone 改成與現行 `brand/conversion` 一致；已執行 `npm run deploy`，Worker 最新版本為 `197802bc-a236-4e1d-905c-f6f08dc0c193`，網址仍為 `<https://lihi-copy-generator-v1.monster7405.workers.dev>`。

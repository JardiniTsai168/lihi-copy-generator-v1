# 2026-07-28

## 文案產生器修正

- 建立修正前備份點：`.backup/pre-fix-20260729-004440.patch`
- 修正 bridge 保護邏輯：`BRIDGE_API_KEY` 存在時不再接受偽造 same-origin header 直接通過；未設定 key 時只允許 local browser request
- 修正 worker proxy：bridge 回傳非 JSON 時，不再直接炸成 generic 500；會回傳結構化 JSON 錯誤並保留 upstream status / 訊息
- 修正模型輸出 URL 安全：母稿與渠道 formatter 回傳的 URL 現在必須通過公開 `https` 網址限制，否則退回安全的 default URL
- 新增前端 checkbox `台灣食品廣告合規模式`：勾選後會把 `complianceMode` 一路送到 generate / format-copy
- 後端新增台灣食品廣告合規模式：prompt 會明確避開高風險與禁用詞，並在母稿 / 主文案 / 渠道文案輸出後再做一次關鍵詞清洗與 fallback
- 前端 mock / fallback 文案也補上 compliance mode 分支，避免後端暫時失敗時又掉回高風險寫法
- 更新靜態資產版本：`public/index.html` 改載 `app.js?v=20260728c`

## 驗證

- `npm test`：6 tests pass
- `node --check bridge-server.js`：pass
- `node --check src/worker.js`：pass
- `node --check public/app.js`：pass

## 待辦

- 後續可補 worker proxy 的單元測試，覆蓋 upstream 非 JSON / 空 body / 200 但格式錯誤等情境
- 若要正式上線到 `copy.bktsai.link` / `copy.bktsai.com`，還需要把這批修改 deploy；目前 repo 內已完成但尚未驗證 live

# 2026-08-02

## creative 素材風格改版上線

- 使用者要求把素材風格中的 `editorial` 拔掉，僅保留 4 種：
  - `clean`
  - `bold`
  - `warm`
  - `luxury`
- 使用者明確指出問題：
  - 同一風格多產幾張時，設計與背景色太像
  - 需要在 prompt 內加入可 random 的選項
- 已在 `lihi-copy-generator-creative-v1` 實作：
  - `creative-engine.js` 改為 4 風格設定
  - 每次生成時，會在每個風格下隨機抽一組變體：
    - 構圖
    - 背景
    - 色彩
    - 光線
    - 輔助元素
    - 文字版位
  - prompt 會明確帶入本次抽中的組合，不再只是固定風格描述
  - 同次 request 內統一使用同一組變體，避免 prompt / fallback 不一致
- 前端同步更新：
  - `public/index.html` 移除 `editorial`
  - `public/app.js` 的素材風格 meta 改為 4 種
- 測試結果：
  - `npm test`：39 tests 全數通過
  - `node --check creative-engine.js` 通過
  - `node --check public/app.js` 通過
  - `node --check public/index.html` 不適用，Node 無法檢查 HTML 副檔名
- deploy：
  - 已執行 `./scripts/deploy-creative-live.sh`
  - `creative-v1` PM2 restart 成功
  - health check 回傳：
    - `{"ok":true,"service":"beck-copy-engine","mode":"live","provider":"openai","model":"qwen/qwen3.5-plus-02-15","framework":"beck-copy-framework-v1"}`

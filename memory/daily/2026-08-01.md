# 2026-08-01

## copy live deploy

- 使用者要求直接把目前 `lihi-copy-generator-v1` 正式版 deploy 上去。
- 本次待部署變更聚焦在 `bridge-server.js` 的簡體轉繁體正規化補強，並同步補上 `tests/bridge.test.js` 覆蓋。
- 部署前已執行 `npm test`：
  - 30 tests 全數通過
  - 新增案例包含「专业营养团队已为你整理这页重点，帮助用户更快读懂。」會正規化為繁中
- 已執行 `./scripts/deploy-live.sh`，目標：
  - host: `copy-live-do`
  - remote dir: `/var/www/beck-v1`
  - pm2 app: `beck-v1`
- 部署結果：
  - `public/` 已同步
  - `bridge-server.js`、`package.json`、`package-lock.json` 已同步
  - `pm2 restart beck-v1 --update-env` 成功
- live 驗證：
  - `https://copy.bktsai.link/health` 回傳 `{"ok":true,"service":"beck-copy-engine","mode":"live","provider":"openai","model":"qwen/qwen3.5-plus-02-15","framework":"beck-copy-framework-v1"}`
  - 正式服務已恢復 online

## creative 下一版 repo 切分

- 使用者確認這一版已交付工程師，下一版將改在 `creative.bktsai.link` 發佈。
- 已決定把下一版切成獨立 GitHub repo，避免和已交付的 `lihi-copy-generator-v1` 共用同一條開發線。
- 新 repo：
  - `JardiniTsai168/lihi-copy-generator-creative-v1`
  - `<https://github.com/JardiniTsai168/lihi-copy-generator-creative-v1>`
- 建 repo 方式：
  - 以目前已提交的 `HEAD` 作為起點建立新資料夾
  - 初始化新的 git 歷史
  - 清除 workspace 本機 artifact：`.openclaw/`、`.runtime/`、`memory/`、`AGENTS.md`、`SOUL.md` 等不適合公開的檔案
  - 補上 `.gitignore` 避免這些內部檔再次進 repo
- 新 repo 目前 local path：
  - `/Users/tonytsai/.openclaw/workspace-lihi-copy-generator-creative-v1`

## creative live 同步到 copy 版

- 使用者要求先把 `copy.bktsai.link` 現在的正式內容完整同步到 `creative.bktsai.link`，再開始繼續開發 creative 線。
- 已在 live 主機上直接以 `/var/www/beck-v1/` 同步覆蓋 `/var/www/creative-v1/`。
- 同步策略：
  - 保留 `creative-v1` 自己的 `.env`
  - 排除 `node_modules`
  - 排除 `backups`
  - 其餘正式內容以 `beck-v1` 覆蓋 `creative-v1`
- 同步前已先在 `creative-v1/backups/<timestamp>/` 備份既有 `bridge-server.js`、`package.json`、`package-lock.json` 與 `public/`
- 已執行：
  - `node --check /var/www/creative-v1/bridge-server.js`
  - `node --check /var/www/creative-v1/public/app.js`
  - `pm2 restart creative-v1 --update-env`
- 驗證結果：
  - `https://creative.bktsai.link/health` 回 `ok: true`
  - `https://copy.bktsai.link` 與 `https://creative.bktsai.link` 首頁 `sha256` 一致
  - `bridge-server.js`、`public/app.js`、`public/index.html`、`public/styles.css` 在 `beck-v1` 與 `creative-v1` 的 `sha256` 全部一致

## creative studio 第一版素材流程

- 使用者確認直接沿用 `creative.bktsai.link` 目前頁面，右側輸出區改成雙主 tab：
  - `產出文案`
  - `產出素材`
- 本次已先實作第一版素材流程：
  - 主文案完成後，自動帶入素材 tab
  - 使用者可選素材風格與模特兒設定
  - 先產 `Facebook 主圖`
  - 主圖完成後可切 `IG`、`Threads`、`Google Ads`
- 技術實作：
  - `public/index.html` 新增 creative studio UI
  - `public/app.js` 新增素材 state、素材 tab 流程、`/api/generate-creative` 串接
  - `src/worker.js` 與 `server.js` 新增 mock creative generator，先回傳 deterministic SVG 素材預覽
- 這版先把流程打通，之後可再把 `generate-creative` 從 mock SVG 換成真 AI 圖像生成。

## creative / copy 開發線隔離

- 使用者明確要求：
  - `copy.bktsai.link` 之後不要再動
  - creative 功能只在 `creative.bktsai.link` 開發與部署
- 已確認正式機原本就分成兩個目錄：
  - `beck-v1` -> `copy.bktsai.link`
  - `creative-v1` -> `creative.bktsai.link`
- 這次真正補強的是本機開發線隔離：
  - 已把目前 creative 相關程式同步進獨立 repo
  - local path: `/Users/tonytsai/.openclaw/workspace-lihi-copy-generator-creative-v1`
  - git remote: `https://github.com/JardiniTsai168/lihi-copy-generator-creative-v1.git`
- 為避免手滑誤 deploy 到 copy：
  - creative repo 的 `scripts/deploy-live.sh` 已改成直接拒絕執行
  - creative repo 僅允許使用 `scripts/deploy-creative-live.sh`
- 驗證：
  - creative repo 已安裝依賴
  - `npm test` 30/30 pass
  - `public/app.js`、`server.js`、`src/worker.js` 語法檢查通過

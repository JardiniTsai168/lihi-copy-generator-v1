# 2026-07-30

## copy / creative live 同步

- 使用者要求把 `copy.bktsai.link` 的正式內容完整複製到 `creative.bktsai.link`，兩邊內容需完全同步。
- 已在同一台 DigitalOcean 主機上，直接以遠端 `rsync` 將 `/var/www/beck-v1/` 同步到 `/var/www/creative-v1/`。
- 同步策略：
  - 保留 `creative-v1` 自己的 `.env`
  - 排除 `node_modules`
  - 排除 `backups`
  - 其餘正式內容以 `beck-v1` 覆蓋 `creative-v1`
- 同步前已先在 `creative-v1/backups/<timestamp>/` 備份既有 `bridge-server.js`、`package.json`、`package-lock.json` 與 `public/`。
- 已同步的關鍵 live 檔案包含：
  - `bridge-server.js`
  - `public/app.js`
  - `public/index.html`
  - `public/styles.css`
- 已執行：
  - `node --check /var/www/creative-v1/bridge-server.js`
  - `node --check /var/www/creative-v1/public/app.js`
  - `pm2 restart creative-v1 --update-env`
- live 驗證：
  - `https://creative.bktsai.link/health` 回 `ok: true`
  - `bridge-server.js`、`public/app.js`、`public/index.html`、`public/styles.css` 在 `beck-v1` 與 `creative-v1` 的 `sha256` 完全一致
- 補充：
  - 遠端 `diff -qr` 僅剩 `lihi-shared` symlink 在該環境下顯示 `No such file or directory`，不影響本次 live 服務同步結果。

## 繁中輸出補強

- 針對使用者回報仍偶見簡體中文殘留，已補強 `bridge-server.js` 的 `SIMPLIFIED_TO_TRADITIONAL_REPLACEMENTS`。
- 本次新增較常出現在行銷文案與介面語句的簡體字轉換，包含：
  - `专 -> 專`
  - `帮 -> 幫`
  - `读 -> 讀`
  - `页 -> 頁`
  - `欢 -> 歡`
  - `选 -> 選`
  - `适 -> 適`
  - `营 -> 營`
  - `销 -> 銷`
  - `经 -> 經`
  - `结 -> 結`
  - `网 -> 網`
  - `联 -> 聯`
  - `讯 -> 訊`
  - `术 -> 術`
  - `数 -> 數`
  - `据 -> 據`
- 已新增測試覆蓋：
  - 單純文字簡轉繁
  - `parseChannelCopyOutput()` 遇到較完整簡體行銷句時會轉成繁體
- 待執行驗證：
  - `npm test`

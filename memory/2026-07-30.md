# 2026-07-30

- `copy.bktsai.link` 是目前正式可用的 live 網址；部署主機為 DO droplet `157.245.151.126`。
- 已建立不依賴 `ssh-agent` 的專用 deploy key：本機 `~/.ssh/id_ed25519_copy_live`，SSH alias 為 `copy-live-do`；遠端已加入 `/root/.ssh/authorized_keys`。
- 已新增部署腳本 `scripts/deploy-live.sh`，可直接走 `copy-live-do` 這條線做 live deploy / dry-run。
- bridge 正式站曾出現 `forbidden_origin`；修法是在 `bridge-server.js` 支援 `BRIDGE_ALLOWED_ORIGINS`，目前 live `.env` 已設 `BRIDGE_ALLOWED_ORIGINS=https://copy.bktsai.link`。
- `generate-copy` 主文案結果快取已移除，現在每次按「產出文案」都會重新生成新文案；頁面分析快取保留。
- 目前表單已調整為：`產品優點4` 放在 `產品優點3` 旁邊、取消「補充第 4/5 個優點」展開區、常駐顯示 `產品優點4/5`，並新增 optional 欄位「其他想補充的內容」；此欄位已接入前後端 prompt。
- 目前 live 已上 10 種濃縮風格 preset，包含：回家療癒版、分享時刻版、童年回憶版、高級品牌版、創辦人故事版、社群口碑版、場景解決方案版、理性對比版、送禮推薦版、限時轉單版。
- 10 種風格是新增 `風格版本` 選單實作，並保留原本 `品牌型 / 轉單型` 與感性 / 理性強度控制。
- 可回退的 live snapshot：`/var/www/beck-v1/backups/rollback-points/live-20260729-165106.tar.gz`；此版本包含 `forbidden_origin` 修正、重新生成新文案、優點欄位常駐化、以及「其他想補充的內容」欄位。
